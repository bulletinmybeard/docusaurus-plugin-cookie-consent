import { en } from './en';
import { de } from './de';
import type { CookieConsentTranslations } from '../plugin/types';

export const builtInTranslations: Record<string, CookieConsentTranslations> = {
    en,
    de
};

export { en, de };
